# react-export-as-image

A lightweight React 18 hook to export any component as an image.

✔ React 18 compatible  
✔ PNG / JPEG / SVG  
✔ TypeScript support  
✔ No legacy APIs  

---

## Install

```bash
npm install react-export-as-image
